{
  "name": "sports-app",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "jest": "^26.6.3",
    "lerna": "^3.22.1",
    "ts-jest": "^26.4.3",
    "typescript": "^4.2.4",
    "@typescript-eslint/eslint-plugin": "^4.15.0",
    "@typescript-eslint/parser": "^4.15.0",
    "eslint": "^7.20.0",
    "eslint-config-prettier": "^7.2.0",
    "eslint-plugin-prettier": "^3.3.1",
    "prettier": "^2.2.1"
  },
  "scripts": {
    "build": "lerna run build --stream",
    "build:api": "lerna run build --stream --scope api",
    "migrate:latest": "lerna run migrate:latest --stream --scope api",
    "migrate:make": "lerna run migrate:make --stream --scope api",
    "migrate:rollback": "lerna run migrate:rollback --stream --scope api",
    "seed:make": "lerna run seed:make --stream --scope api",
    "seed:run": "lerna run seed:run --stream --scope api",
    "start": "lerna run start --stream",
    "start:api": "lerna run start --stream --scope api",
    "start:api:dev": "lerna run start:dev --stream --scope api",
    "start:ui": "lerna run start --stream --scope ui",
    "lint": "lerna run lint --stream",
    "test": "jest"
  }
}
