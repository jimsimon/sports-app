{
  "name": "sports-app",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=7.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@testing-library/dom": "^7.21.4",
    "@testing-library/jest-dom": "^5.11.1",
    "@testing-library/user-event": "^12.0.14",
    "@types/jest": "^26.0.7",
    "jest": "^26.1.0",
    "lerna": "^3.22.1",
    "ts-jest": "^26.1.3",
    "typescript": "^3.9.7"
  },
  "scripts": {
    "build": "lerna run build --stream",
    "build:api": "lerna run build --stream --scope api",
    "build:ui": "lerna run build --stream --scope ui",
    "start": "lerna run start --stream",
    "start:api": "lerna run start --stream --scope api",
    "start:ui": "lerna run start --stream --scope ui",
    "test": "jest"
  },
  "prisma": {
    "schema": "packages/api/prisma/schema.prisma"
  }
}
